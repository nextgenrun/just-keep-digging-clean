# Components

UI module — components.

`NewRunSetupPanel.js` is the approved-bitmap empty-slot setup surface. It owns
Casual/Hardcore selection, Guided/Skip selection, typed `YES` skip confirmation,
and the hidden One-Life sequence. It emits pure selections; values modules
create persisted data and StartMenu owns navigation.
