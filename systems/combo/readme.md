# Combo

Game system — combo.

Combo count, decay timing, multiplier, and milestone GP restores remain owned
by `ComboSystem.js`. Milestone reward feedback stays visible, but a combo break
does not create a transient notification card.
