import { MOVING_SIDE_DIG_PRODUCTION } from "./movingSideDigAnimation.generated.js";

// The generated module is emitted by the Piskel production builder from
// movingSideDigProduction.json. Runtime code consumes the same exact phase,
// anchor, contact, and rollback contract that produced the sheets.
export const MOVING_SIDE_DIG_ANIMATION = MOVING_SIDE_DIG_PRODUCTION;
