# Torch HUD alignment and mining mockups — 2026-09-06

The torch HUD correction is explicitly approved by the user and implemented. The new integrated player-core artwork has a wide value compartment and a narrow icon compartment. The former cropped legacy HUD contained another frame and a fixed key badge. ApprovedHudSkin now uses the clean approved torch atlas icon. TorchIntensityControl centers a fitted 1–200% readout, a compact title, and an explicit ON/OFF state in the value compartment. TorchIntensityTooltip uses the approved baked panel, current key binding, existing scroll/click steps, and authoritative GP drain. Hover exit, hidden controls, and pause close the tooltip. Torch gameplay is unchanged.

Runtime verification uses canonical serve.py on port 8196 and an isolated Chrome context with save writes blocked. It checks 1%, 100%, 200%, live wheel 100→101→100, click 100→110, actual T toggling, hover exit, pause, and 1280×720 / 800×450 / 1600×1000 / 1920×1080 views. The final run reported no page errors. Dynamic torch and the HP/GP/merchant presentation contracts pass. The old torch contract's legacy crop and historic buff-position expectations were updated to the current approved icon and containment rules.

The user rejected all mining directions in this review, saying they look much more boring. The terrain replacement, scenery reduction, ore blending, tunnel-edge pass, and player lighting/glow must not be promoted. They were never wired into production. testing/2026-09-06-mining-readability-review.js is imported only by the isolated capture helper. It reuses the weathered-roots rain-compacted-loam material, selects proper frames from the authored terrain-cap atlas, blends existing ore art, and optionally adds player color-matrix brightness and a constant subtle rim. A single staged 41 m chamber is captured with identical camera, player pose/texture/scale, torch state, and 88/88 target HP. No collision, mining, resource, progression, or lighting-radius authority is changed. This frozen-frame rendering proof is not movement or performance acceptance.

Review: ../visual-approval-previews/2026-09-06-torch-and-mining-mockups/review.html
Evidence: torch-runtime-verification.json and mining-review-verification.json in that folder.