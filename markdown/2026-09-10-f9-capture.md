# F9 black-world capture investigation — 2026-09-10

Local-only. The reported broad WebM contains a visible HUD over a black world;
this is visible in decoded FFmpeg frames, not just in one video player.

ScreenRecordFrameSource now reads the completed default WebGL framebuffer,
restores the prior framebuffer binding, flips its rows and reuses its buffers.
ScreenRecordSystem starts frame copying after a rendered frame rather than
immediately during the key event. The Canvas renderer retains direct copying.
No capture buffers or readback work remain after recording teardown.

An embedded browser with an unsupported native prompt now uses the configured
default format. Other browsers retain the SHORT/BROAD chooser and cancellation.

The framebuffer regression checks orientation, correct target/restoration,
buffer reuse, context loss and Canvas fallback. Existing recorder contracts
pass. An actual F9 SHORT recording with the prompt fallback saved successfully
and its extracted image contains the miner and complete world.

Full-width framebuffer recording validation is in progress.

The framebuffer BROAD test saved a valid 1920x1080 gameplay video, inspected as testing/2026-09-10-f9-broad-after.png. That test preceded the UI-exclusion request and includes HUD. Both final mode configs now hide UI; the mode contract verifies hide and exact restore for both formats. The embedded fixture needs focus returned to the game after using its external controls; an unfocused attempt produced a 110-byte empty test clip and is not acceptance evidence.

Focused clean framebuffer capture passed: screenrecord-20260910-023214-239557-screenrecord-short-2026-09-09T23-32-13-531Z.webm (8,989,967 bytes). Its extracted testing/2026-09-10-f9-short-after.png contains gameplay without HUD. The original user's underground 48m condition was not reproduced; verified replacement clips cover the town surface. Live site unchanged.
