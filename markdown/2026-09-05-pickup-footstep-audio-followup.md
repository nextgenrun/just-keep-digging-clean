# Pickup and footstep audio follow-up - 2026-09-05

The user reported intrusive pickup/reward sounds and bad footsteps after the first core-action cleanup. This follow-up addresses ground routing, relative level, repetition and a review-player volume error.

The real PlayerPhysicsBody exposes w/h. The sound material probe was reading width/height, sampling near the top of the body instead of beneath the feet. Consequently, dirt could take the hard-ground footstep fallback and soft landings could take the hard debris sound. The resolver now samples the center of the feet using w/h, retaining compatibility with width/height bodies. A regression instantiates the real PlayerPhysicsBody, stands it across a tile boundary and verifies both footstep and landing routes. The listening fixture also uses the real shape and spatial floor rows; the first-pass fixture used generic dimensions and missed this bug.

Routine resource gain is 0.225 of its prior value, about -13 dB. A special XP cue uses 0.3 and shares the same 850 ms admission window. Routine XP and level-up arrivals no longer add pitched UI ticks; the dedicated approved level-up cue remains intact. Rapid shop purchase and reward entry points share a 700 ms limit and use four existing approved coin recordings from one pack, each shorter than 280 ms. Cold recordings warm silently, and rollback fallbacks receive corresponding quiet trims.

The existing soft and hard footstep bank gains are about 8 dB lower than the first cleanup, with a 240 ms minimum separation. Animation contacts remain authoritative; no player input, physics, speed or reward mechanics change. Reviewed and Freesound cooldowns accept a scene clock rewind rather than waiting for an earlier scene timestamp to catch up.

The review player no longer raises current gains below 0.01 or treats a zero suggestion as missing. Solo inspection remains the explicitly selected source-inspection boost. Physical-file IDs and browser storage keys are unchanged. The active projection contains 341 of the unchanged 562 Freesound approvals, and the refreshed catalog contains 753 clips. Eleven coin paths leave active routing; no subjective rejection was invented for them.

Six short, filtered footstep alternatives are ready for audition only. They use existing recordings, not new AI-generated sounds. The offline pipeline selects the first significant contact envelope, retains a small pre-roll, filters low/high-frequency excess, caps duration at 160-180 ms and fades the edges. Delivery is mono 24 kHz OGG. Exact edit points, hashes and credits are recorded. The exploratory pickup edit removed too much source energy and was omitted from the page. None of these edits replaces a runtime source before listening review.

Open http://127.0.0.1:8080/testing/audio-pickup-footstep-2026-09-05/. The same scripted pickup sequence drops from 30 to 8 sound starts; shop bursts from 12 to 3; mining with loot from 40 to 25. Captures use actual SoundSystem methods and real mining dispatchers, with measured completion, stop times, rates and mixer gain changes. These examples are not a full-game playthrough. Final test details, source preservation and HTTP checks are in final-audit.json. Browser control timed out, so no manual browser or subjective listening acceptance is claimed.
