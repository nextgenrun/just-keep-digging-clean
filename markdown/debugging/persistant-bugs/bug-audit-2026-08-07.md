# Persistent Bug Database

## Scope
- Review pass: `C:\xampp\_Backups\dig-game-simple\dig-game-dev-env-cleaned`
- Date: 2026-08-07
- Focus: active runtime + non-archived source first, then archive/testing debt.
- Policy followed: no gameplay or behavior changes were made; only findings recorded.

## Active runtime findings

| ID | Severity | Location | Issue | Evidence | Impact | Permanent fix |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-2026-08-07-001 | Medium | [`main.js:5-7`](file:///C:/xampp/_Backups/dig-game-simple/dig-game-dev-env-cleaned/main.js), [`index.html:16`](file:///C:/xampp/_Backups/dig-game-simple/dig-game-dev-env-cleaned/index.html), and multiple `*.js` imports with `?rev=` | ES module imports are versioned with URL query strings in source. | `?rev=` appears in active module import specifiers and boot-time script tag. | Fragile on servers/builders that do not preserve query strings for JS module resolution. Can break production bundling or strict import-map tooling. | Normalize all internal imports to plain relative paths (or use a bundler cache invalidation mechanism). Keep cache busting only in static index HTML (if needed) or via build tooling. |
| BUG-2026-08-07-002 | Medium | [`ui/scenes/BootScene.js:721-722`](file:///C:/xampp/_Backups/dig-game-simple/dig-game-dev-env-cleaned/ui/scenes/BootScene.js) | Quickslash asset filenames contain typos/invalid names. | `load.image('char-v2-quickslash-1', "${base}/uickslash-1.webp");` and `uickslash-2 .webp` include misspelling/extra space. | Runtime asset load can fail on case-sensitive file systems or strict URL checks, causing quickslash animation to miss and degrade ability animation playback. | Update filenames in load calls to `quickslash-1.webp` and `quickslash-2.webp`, and verify all references match actual asset names. |

## Archive / legacy debt (non-runtime)

These are not currently part of active runtime entry points, but they are real broken references worth permanent clean-up when legacy content is revisited:

| ID | Severity | Location | Issue | Evidence | Impact | Permanent fix |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-ARC-2026-08-07-001 | Low | `archive\2026-07-10-unused-orphan-modules\` | Multiple files import missing `upgradeCategories.js`. | Unresolved import check reports `./upgradeCategories.js` in several orphan files. | Static audit of archived code shows broken imports if these files are ever reactivated. | Either restore/relocate `upgradeCategories.js` for these archived modules or remove/archive them as intentionally retired. |
| BUG-ARC-2026-08-07-002 | Low | `archive\2026-06-26-systems-cleanup\AboveGroundDecorationSystem.js` | Import `../../values/aboveGroundDecorations.js` not found. | Unresolved import check result. | Same as above. | Resolve reference or gate legacy file as dormant/inactive. |
| BUG-ARC-2026-08-07-003 | Low | `archive\2026-06-26-shadow-miner-future-system\ShadowMinerSystem.js` and related legacy modules | Historical module imports point to files that do not exist under archive path. | Unresolved import check result. | Same as above. | Same as above. |
| BUG-ARC-2026-08-07-004 | Low | `archive\2026-07-10-todo-animation-stubs\*Anims.js` | Imports in `NpcAnims.js`, `PlayerAnims.js`, `RobotAnims.js` resolve to missing `assetKeys.js`. | Unresolved import check result. | Same as above. | Same as above. |

## Additional low-confidence items (testing/non-ship only)

- `testing\animation-sandbox\sandbox.js` and `testing\animation-sandbox\frame-audit\auditConfig.js` also contain the same `uickslash-*` asset naming issue. Keep them aligned if those test harnesses are used for production-facing QA.

## Status update
- Active runtime pass reached no further high-confidence unresolved-import/asset-missing issues after query-string normalization check.
- Remaining issues are marked for separate remediation tracks to avoid mixing active code changes with legacy debt.
