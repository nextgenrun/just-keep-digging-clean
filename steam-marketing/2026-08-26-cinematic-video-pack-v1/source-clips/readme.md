# OpenRouter Source Clips

Raw reference-guided clips downloaded from OpenRouter. These are editing
sources, not direct runtime or platform uploads. `generation-manifest.json` in
the parent directory records prompts, job IDs, model, usage, and output paths.

The API key is never written to this directory.
