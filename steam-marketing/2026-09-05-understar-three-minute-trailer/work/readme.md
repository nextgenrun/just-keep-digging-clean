# Trailer production files

This folder retains the isolated capture director, source inventories, source contact sheets, fonts and logo, normalized edit clips, audio stems, picture lock and render logs. The .blend project uses relative paths into this folder. Keep it alongside the project.

Capture controls were hosted by canonical serve.py on local port8774. Save writes were blocked in the isolated filming session. The capture page is an offline marketing utility and is not linked from the game.
