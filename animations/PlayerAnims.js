/**
 * PlayerAnims — Phaser animation configs for the player character.
 * Frame keys from values/assetKeys.js. To be implemented when spritesheets are finalized.
 */
import { ASSET_KEYS } from "../values/assetKeys.js";

/** Creates all player animations on the given Phaser scene */
export function createPlayerAnims(scene) {
  // TODO: implement once player spritesheet is finalized
  // scene.anims.create({ key: "player-idle", frames: [...], frameRate: 8, repeat: -1 });
  // scene.anims.create({ key: "player-walk", frames: [...], frameRate: 10, repeat: -1 });
  // scene.anims.create({ key: "player-dig", frames: [...], frameRate: 12, repeat: 0 });
  // scene.anims.create({ key: "player-climb", frames: [...], frameRate: 8, repeat: -1 });
  // scene.anims.create({ key: "player-jump", frames: [...], frameRate: 10, repeat: 0 });
}