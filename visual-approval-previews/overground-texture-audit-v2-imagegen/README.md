# Overworld texture audit v2 — opaque ImageGen production

`01-opaque-imagegen-production-contact-sheet.png` is the selected native-game
review sheet.

- All ten resource blocks are new complete ImageGen rasters.
- All reward blocks except Teleport Up are new complete ImageGen rasters.
- Teleport Up is the existing approved
  `sprites/tiles/special-tiles-v2/teleport-tile.webp`.
- Ancient Relic Cache remains unchanged.
- Every production atlas and static tile is opaque; no special-block emissive
  overlay is used.

`reviewOnly: false`; `productionChanged: true`; `selectionStatus: selected`.
