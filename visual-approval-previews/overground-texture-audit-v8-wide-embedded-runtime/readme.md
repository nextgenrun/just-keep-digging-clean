# Wide embedded resource runtime QA V8

This folder contains labeled review evidence showing all six variants of every
production resource over actual runtime ground families at 94 px.

The QA boards are not game-loaded. Production loads only the isolated
transparent v6 atlas frames.
