# Level One Biome Signature Sources V1

Untouched `1536x1024` RGBA PNG outputs from built-in ImageGen, one distinct
generation call for each of the twenty Level One visual biome families.

These lossless files are provenance and rebuild inputs. Runtime uses only the
alpha-gated WebP derivatives in `level1-biome-signatures-v1/`.

