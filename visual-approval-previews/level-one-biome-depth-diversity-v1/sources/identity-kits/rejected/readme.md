# Rejected Identity-Kit Sources

These two first-pass sources are retained as provenance evidence only. Their
paint crossed the atlas center gutter, so neither is referenced by the runtime
or final manifest. Corrected independent ImageGen results occupy the canonical
source paths one directory above.
