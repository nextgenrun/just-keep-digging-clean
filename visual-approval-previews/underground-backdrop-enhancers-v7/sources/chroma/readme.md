# Chroma sources

These are the untouched, renamed 1536 x 1024 ImageGen source PNGs on a sampled
green key. They are retained for visual provenance and selective regeneration.

The production builder never treats the green pixels as authored game color.
