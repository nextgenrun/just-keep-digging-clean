# Player voice V2 - unapproved design draft

The user requested a design audit and style mockups before any generation on
2026-09-07. No new recordings were produced: the first request failed with an
expired API key. Generation is now explicitly held for design review.

The 24-line source is provisional, not an accepted library. Its runtime default
is disabled, and the normal game still imports V1. Do not activate this draft
or regenerate it until the user settles the voice design.

The conversation mockup compares Grounded miner, Dry humour, Curious explorer,
and Minimal callouts across confirmed discoveries, danger, returns, and quiet
routine events. It includes alternate takes and is text only.
