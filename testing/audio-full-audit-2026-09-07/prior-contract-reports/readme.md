# Isolated existing contract reports

run-existing-contract.mjs preserves all existing assertions and redirects only report writes into this dated directory. Historical reports are not replaced. Test filenames prefix outputs.
