# Published Hints and wiki proof

Captured through the clean public Nextgen URLs on 7 September 2026 in isolated browser profiles. `gameplay.json` confirms the 15-entry Hints library and no runtime errors; the game test also checks paging, reopening, keyboard use, the wiki popup, and returning to gameplay. `wiki-results.json` records the seven real search queries, with 1440/768/390 px layout and direct-link checks.

The wiki initially returned older cached HTML. The final browser run passed against the refreshed clean URL. An earlier failure screenshot is retained as diagnostic history. Raw command-line requests also encountered SiteGround's access challenge; browser checks and SSH file hashes provide the delivery evidence.
