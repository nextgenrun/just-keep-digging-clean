# Footstep contact follow-up

Scoped baseline and evidence for the subsequent hard-ground/contact-length and stale mining-preview correction. Files named with double underscores are the source state before this follow-up; they are not current runtime modules.

`verify.mjs` checks real foot-contact routing, short segment attacks and comparable hard-ground peaks, review identity/reject exclusion, and withdrawal of misleading mixed previews. `verification.json` records these checks; `regressions.json` records the ten existing focused contracts. `browser-hard-walk.json` and `browser-dirt-walk.json` record native playback on the actual walk animation. `served-files.json` verifies canonical HTTP and the unchanged source export. See the parent readme for behavior and listening limitations.
