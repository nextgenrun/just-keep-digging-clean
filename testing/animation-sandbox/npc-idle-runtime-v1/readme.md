# NPC Idle Runtime v1

Phaser verification scene for the five approved game-time merchant WebMs. It uses the same video loader, video game object, created-event sizing, and loop playback pattern as `BootScene` plus `NPCManager`.

Open through the project server:

`http://127.0.0.1:8080/testing/animation-sandbox/npc-idle-runtime-v1/index.html`

