# Infinite V4 previews

Review-only GIFs rebuilt from the 44 existing V4 character proofs. Every file
uses infinite replay so one-shot transitions, attacks and reactions can be
audited repeatedly. `manifest.json` records the source preview and frame count.
The packager removes only the connected pure-black render background so dark
character materials remain intact against the shared audit stage.
