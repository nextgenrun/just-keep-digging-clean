# Rejected direct loop crops

These background-only crops prove that all three raw sources were camera-stable
and excluded the ground, but their 8.10–10.85 MAE endpoint jumps were not
seamless. They remain as rejected comparison evidence. The review page uses the
corrected forward-only files under `../loops-v3/`.
