# Endpoint-anchor retry record

This directory records a no-cost failed retry using distinct start/end files
whose animated regions were pixel-identical. OpenRouter rejected the job before
generation because the throwaway key had reached its total limit. No video or
API key is stored here.
