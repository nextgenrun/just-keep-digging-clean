# Freesound orchestra volume evidence

Six reproducible local WAV overlap tests from `pipelines/audio/auditFreesoundOrchestra.py`.
These are diagnostic renders, not additional gameplay assets or new approvals.
Read `../freesound-rendered-volume-audit.json` for exact source IDs, gains,
bus limits, sample peaks, 4x-resampled peaks and the audit's exclusions.
