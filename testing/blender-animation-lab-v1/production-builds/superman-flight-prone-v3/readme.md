# Superman prone-v3 production build

Production evidence for the versioned runtime sheet generated from the exact `DG_SUPERMAN_FLIGHT_IDLE_PRONE_V3` Blender pose. The old Push Loop sheet remains untouched for rollback.

The 2026-08-15 contract repair restores the exact prone-v3 sheet after the
quality-v1 replacement changed facing and silhouette placement without matching
the authored rig contract. The rejected quality-v1 hash remains recorded in the
promotion manifest and its byte-exact files remain recoverable in
`archive/2026-08-15-survival-animation-contract-repair-v2/`.
