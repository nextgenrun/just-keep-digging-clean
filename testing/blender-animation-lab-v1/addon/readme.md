# Blender Animation Lab add-on

`dig_game_animation_lab` is a Blender 5.1 package. It separates session setup,
pose authoring, camera-space hitbox review, prop attachments, candidate mesh
fitting, review export, and UI into small single-purpose modules.

The add-on intentionally contains no production-promotion operator.

