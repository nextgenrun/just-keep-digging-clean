# Legacy Miner walk fit v2 - corrected review candidate

This is an isolated correction of the visibly broken v1 proof. It uses the real Meshy GLB and native weights. The spine is mapped by actual hierarchy, animation is solved from joint directions in a fitted common coordinate frame, pelvis sway is scaled, and foot contact is grounded. Nothing here is loaded by the game runtime.

The proof renders include the source Survival motion, the failed v1 Meshy transfer, and this corrected v2 Meshy transfer at identical frames.
