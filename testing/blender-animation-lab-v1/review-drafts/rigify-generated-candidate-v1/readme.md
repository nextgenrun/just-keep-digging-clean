# Rigify generated candidate v1

Review-only generated Rigify control/deformation rig built from an immutable
user-edited metarig snapshot. Production skin weights are transferred by exact
vertex index into matching Rigify deformation groups. The live metarig
workbook, source rig, actions and runtime remain untouched.
