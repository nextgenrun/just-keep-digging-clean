# Unreal Automation Scripts

Python scripts executed by `UnrealEditor-Cmd` to import the Legacy Miner, create the IK retarget assets, assemble the walk-only review level, render candidate angles, and export validation metadata.
