# Source Assets

Local source files imported into the walk-only Unreal sandbox. The Meshy GLB is a generated character source; exported FBX files are derived interchange files for Unreal import. No Unreal Engine `.uasset` is copied into this directory.
