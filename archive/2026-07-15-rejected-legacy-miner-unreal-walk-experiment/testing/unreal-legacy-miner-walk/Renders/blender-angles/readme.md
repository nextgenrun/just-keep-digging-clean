# Blender Camera Candidates

Camera-only polish candidates rendered from the FBX exported from `LegacyMiner_Unreal_Walk`. Blender does not author or replace the animation; it supplies the fixed textured presentation camera after Unreal retargeting.
