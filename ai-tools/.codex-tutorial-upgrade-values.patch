diff --git a/values/retentionConfig.js b/values/retentionConfig.js
--- a/values/retentionConfig.js
+++ b/values/retentionConfig.js
@@ -12,6 +12,7 @@
   UNSELECTED: "unselected",
   MOVE: "move",
   DIG: "dig",
+  UPGRADE: "upgrade",
   FLIGHT: "flight",
   PORTAL: "portal",
   SELL: "sell",
@@ -24,3 +25,3 @@
-  saveVersion: 3,
+  saveVersion: 4,
@@ -43,8 +44,9 @@
     activeStages: Object.freeze([
       TOWN_TUTORIAL_STAGES.MOVE,
       TOWN_TUTORIAL_STAGES.DIG,
+      TOWN_TUTORIAL_STAGES.UPGRADE,
       TOWN_TUTORIAL_STAGES.FLIGHT,
       TOWN_TUTORIAL_STAGES.PORTAL,
       TOWN_TUTORIAL_STAGES.SELL,
       TOWN_TUTORIAL_STAGES.RESUME,
@@ -58,8 +60,12 @@
     merchants: Object.freeze({
       sell: "moneyMonster",
+      upgrade: "playerUpgrades",
+    }),
+    upgradeFunding: Object.freeze({
+      money: 2,
     }),
     flightTraining: Object.freeze({
       flightUpgradeId: "gemPowerUnlock",
       freeFlightMs: 30000,
@@ -67,40 +73,45 @@
     choice: Object.freeze({
       title: "DO YOU WANT TO PLAY THE TUTORIAL?",
-      body: "A short guided start through movement, digging, Flight,\nyour first return gate, selling, and resuming the route.\n\nSkip it if you know the loop — Flight is still unlocked.",
+      body: "A short guided start through movement, digging, selling,\nyour first upgrade, Flight, and a permanent return gate.\n\nSkip it if you know the loop — Flight is still unlocked.",
       yesLabel: "YES  •  TEACH ME",
       noLabel: "NO  •  START PLAYING",
       footer: "A / D OR ARROWS  CHOOSE     ENTER  CONFIRM     ESC  BACK",
     }),
     copy: Object.freeze({
       move: Object.freeze({
-        phase: "1 / 6  •  MOVE",
+        phase: "1 / 7  •  MOVE",
         title: "GET COMFORTABLE IN TOWN",
         body: "{left}/{right} move and aim  •  {interact} talks to people",
       }),
       dig: Object.freeze({
-        phase: "2 / 6  •  DIG",
+        phase: "2 / 7  •  DIG",
         title: "BREAK THE PRACTICE BLOCK",
         body: "Walk to the mining marker  •  face the block  •  hold {mine} to dig",
       }),
+      sell: Object.freeze({
+        phase: "3 / 7  •  SELL",
+        title: "TURN YOUR FIRST CARGO INTO MONEY",
+        body: "Return to the Money Monster  •  press {interact}  •  sell any stack",
+      }),
+      upgrade: Object.freeze({
+        phase: "4 / 7  •  UPGRADE",
+        title: "BUY AGILITY TRAINING",
+        body: "Visit Upgrades  •  press {interact}  •  buy the movement-speed upgrade",
+      }),
       flight: Object.freeze({
-        phase: "3 / 6  •  FLIGHT",
+        phase: "5 / 7  •  FLIGHT",
         title: "PROVE LOCAL RECOVERY",
         body: "Hold {fly} until you lift off  •  then descend toward 40m",
       }),
       portal: Object.freeze({
-        phase: "4 / 6  •  RETURN GATE",
+        phase: "6 / 7  •  RETURN GATE",
         title: "OPEN A PERMANENT ROUTE HOME",
         body: "Follow the starter route to 40m  •  press {interact} at the gate",
       }),
-      sell: Object.freeze({
-        phase: "5 / 6  •  SELL",
-        title: "TURN REAL CARGO INTO MONEY",
-        body: "Return to the Money Monster  •  press {interact}  •  sell any stack",
-      }),
       resume: Object.freeze({
-        phase: "6 / 6  •  RESUME",
+        phase: "7 / 7  •  RESUME",
         title: "REOPEN THE DEEP ROUTE",
         body: "Use the surface gate  •  enter the paired sky gate  •  resume at 40m",
diff --git a/values/firstFiveMinutes.js b/values/firstFiveMinutes.js
--- a/values/firstFiveMinutes.js
+++ b/values/firstFiveMinutes.js
@@ -20,7 +20,28 @@
   surfaceSafety: Object.freeze({
     blockedDetailMs: 1800,
     depthGuardTolerancePx: 4,
     recoveryScanRadiusTiles: 12,
     safeReturnTileX: 4,
   }),
+  flightReminders: Object.freeze({
+    count: 3,
+    intervalMs: 30000,
+    windowMs: 90000,
+    notificationKeys: Object.freeze({
+      unlock: "first-five-flight-unlocked",
+      reminder: "first-five-flight-reminder",
+    }),
+    copy: Object.freeze({
+      unlock: Object.freeze({
+        phase: "FLIGHT UNLOCKED",
+        title: "YOU UNLOCKED FLIGHT",
+        body: "PRESS {fly} TO FLY  •  KEEP HOLDING UNTIL YOU LIFT OFF",
+      }),
+      reminder: Object.freeze({
+        phase: "FLIGHT REMINDER",
+        title: "PRESS {fly} TO FLY",
+        body: "KEEP HOLDING {fly} UNTIL YOU LIFT OFF",
+      }),
+    }),
+  }),
   copy: Object.freeze({
diff --git a/values/systemIntroduction.js b/values/systemIntroduction.js
--- a/values/systemIntroduction.js
+++ b/values/systemIntroduction.js
@@ -69,6 +69,6 @@
   upgradeUnlocks: Object.freeze({
     minersGrip: "core",
     gemPowerUnlock: "flightReady",
-    agility: "firstReturn",
+    agility: "core",
     strength: "gearMerchant",
diff --git a/values/tutorialNarration.js b/values/tutorialNarration.js
--- a/values/tutorialNarration.js
+++ b/values/tutorialNarration.js
@@ -28,35 +28,41 @@
+  sell: cue(
+    "sell",
+    "Sell the cargo you just mined. That money funds your first real upgrade.",
+    "STEP 3  •  SELL YOUR FIRST CARGO",
+    "RETURN TO THE MONEY MONSTER  •  {interact}  •  SELL 1+ ITEM",
+  ),
+  upgrade: cue(
+    "upgrade",
+    "Buy Agility Training. The movement-speed increase stays with you.",
+    "STEP 4  •  BUY {upgrade}",
+    "VISIT UPGRADES  •  {interact}  •  BUY THE MOVEMENT-SPEED UPGRADE",
+  ),
   flight: cue(
     "flight",
     "Hold Flight to rise. Use it whenever you need to recover locally.",
-    "STEP 3  •  PROVE YOUR LOCAL RECOVERY",
+    "STEP 5  •  PROVE YOUR LOCAL RECOVERY",
     "HOLD {fly} UNTIL YOU LIFT OFF  •  THEN DESCEND TOWARD 40M",
   ),
   portal: cue(
     "portal",
     "Descend to the return gate. Activating it opens a permanent route home.",
-    "STEP 4  •  FIND THE RETURN GATE AT 40M",
+    "STEP 6  •  FIND THE RETURN GATE AT 40M",
     "DIG DOWN THE STARTER ROUTE  •  UNLOCK A PERMANENT WAY HOME",
   ),
   portalNear: cue(
     "portal-near",
     "That gate is your way home. Activate it once and it stays unlocked.",
     "RETURN GATE NEARBY",
     "FOLLOW ITS LIGHT  •  PRESS {interact} WHEN YOU REACH IT",
   ),
-  sell: cue(
-    "sell",
-    "Your route is open. Return to town and sell what you actually found.",
-    "STEP 5  •  SELL YOUR REAL CARGO",
-    "RETURN TO THE MONEY MONSTER  •  {interact}  •  SELL 1+ ITEM",
-  ),
   resume: cue(
     "resume",
     "The surface gate now returns you to the route you opened.",
-    "STEP 6  •  RESUME AT 40M",
+    "STEP 7  •  RESUME AT 40M",
     "USE THE MARKED SURFACE GATE  •  THEN ENTER YOUR PAIRED SKY GATE",
