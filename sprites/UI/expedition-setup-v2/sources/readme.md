# Expedition Setup V2 Sources

These are the untouched built-in ImageGen masters for the unified empty panel
and its transparent selected-state rim. Runtime text, icons, selected choices,
and click behavior are deliberately absent from the sources.
