# Pickaxe HUD and GP theme mockup prompt v1

Mode: built-in ImageGen.

Use case: `ui-mockup`.

Input images:

1. `sprites/UI/hud-approved-v1/player-core.png` — locked structural reference
   for the current top-left HUD panel.
2. `sprites/UI/pickaxe-icons-v1/pickaxe-progression-contact-sheet-v1.png` —
   exact approved seven-pickaxe family and tier-palette reference.

## Final prompt

Create a polished 16:9 game-UI review board showing how the current purchased
pickaxe becomes permanently visible in the top-left HUD and lightly themes the
Gem Power bar. Preserve the current compact horizontal proportions and its
established blackened iron, aged brass, dark leather, and restrained fantasy
materials. Do not redesign the whole HUD.

The left circular medallion contains the currently equipped pickaxe. Retain the
depth readout above the GP bar and retain the torch and `T` key at right. Add a
very small current-tool identity cue near the depth line, formatted as a compact
tier name such as `DRAGON VII`, plus seven tiny tier rivets or pips. The GP bar
must remain unmistakably purple Gem Power, but gain restrained
material-colored edge light, engraved end caps, and a subtle repeating accent
motif taken from the current pickaxe. Do not make the GP energy a solid tier
recolor.

Show one large hero example using Dragon Pickaxe with exact readable text
`DEPTH 0 m`, `DRAGON VII`, and `GP 105 / 150`. Below it, show seven compact
comparison variants labeled exactly `BRONZE I`, `IRON II`, `STEEL III`,
`MITHRIL IV`, `ADAMANT V`, `RUNE VI`, and `DRAGON VII`, each using the
corresponding silhouette and restrained accent palette from the pickaxe
reference. Include a small 1:1 runtime-size proof. Use a dark neutral
presentation background, practical production UI, crisp readable hierarchy,
and no concept-art scenery.

Keep the original panel footprint, layout logic, black/gold visual language,
torch location, depth priority, and GP readability. Pickaxe identity must be
obvious without making the panel larger. No extra buttons, currencies,
character portraits, logos, or watermark.
