# Mining Target V1 Sources

ImageGen source and transparent-master files for the production mining-target
overlay. These files are provenance and rebuild inputs only; BootScene loads
only the optimized WebP file in the parent directory.

The flat magenta source backgrounds were removed with the installed ImageGen
chroma-key helper using a soft matte and despill.
