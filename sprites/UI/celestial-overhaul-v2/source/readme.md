# ImageGen Sources

These source images preserve the built-in ImageGen outputs used for the V2
Celestial talent-tree foundation, node frame, connector, and hover tooltip.
Magenta sources are processed with the installed chroma-removal helper. The
tooltip builder removes its generated light checkerboard before runtime use.
