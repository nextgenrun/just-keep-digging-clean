# Surface Props V2 Alpha Masters

Lossless PNG alpha masters produced from the retained ImageGen chroma sources
with the installed `remove_chroma_key.py` helper.

These files are build provenance and are not preloaded by the game. The
date-stamped surface-prop builder trims and encodes the runtime WebPs one
directory above.
