# Untouched ImageGen sources

These are byte-identical copies of the generated tree trunk and living/dead
foliage masters. They are not loaded by the game. Never overwrite them with a
processed derivative. See the parent `generation.json` for prompts/provenance.
