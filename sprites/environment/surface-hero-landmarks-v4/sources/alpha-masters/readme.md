# Surface Hero Landmark V4 Alpha Masters

This folder retains the seven full-canvas, alpha-cleaned masters used to build
the cropped runtime cutouts two directory levels above.

They are never loaded by Phaser. The dated preparation tool crops only
transparent padding, validates the correct green or magenta residual key,
protects the Starwell opening, and refuses to overwrite an existing runtime
asset unless the rebuilt pixels match exactly.