# Surface Hero Landmark Sources

Flat chroma-key ImageGen sources retained for reproducible alpha extraction.
Six assets use `#00ff00`; the foliage-bearing Frontier pavilion uses `#ff00ff`.
These files are build provenance and are never preloaded by Phaser.