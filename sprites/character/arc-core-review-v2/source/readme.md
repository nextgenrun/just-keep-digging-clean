# Arc Core Review V2 Sources

Immutable ImageGen source masters for the drift-locked Arc Core review package.
The `*-chroma.png` files are original built-in ImageGen outputs. The matching
`*-alpha.png` files were extracted with the installed ImageGen chroma-key
helper using border sampling, a soft matte, and despill.

Do not load these large source files at runtime.
