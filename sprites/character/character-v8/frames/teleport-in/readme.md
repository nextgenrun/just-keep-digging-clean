# Teleport In Curated Source

The loose PNG files are the authoritative animation selection. Frames 01-08 and 14-43 are intentionally retained; frames 09-13 were removed during curation and must not be restored automatically.

Process the selected frames with:

```powershell
python tools/build_legacy_miner_v8_runtime.py --ids teleport-in
```
