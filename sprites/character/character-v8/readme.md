# Character V8 Review Assets

Sandbox-only character animation sources, frames, sheets, previews, Piskel files, and reports described by `character-v8-manifest.json`.

The manifest is authoritative for frame size, animation timing, source provenance, and output paths. These assets are not automatically wired into the Phaser player runtime.
