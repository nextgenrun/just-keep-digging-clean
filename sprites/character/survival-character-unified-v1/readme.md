# Unified Survival character

`runtime/` contains the active lossless character sheets and their build manifest.
Read its README before changing the package. Gameplay contact timing is owned by
the animation mappings in `values/`; presentation effects must use coordinates
from these current sheets, not the rejected legacy rig-marker manifest.
