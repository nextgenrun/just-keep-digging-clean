# Character Piskel manifest

`character-animation-manifest.json` registers editable `.piskel` sources,
runtime exports, previews, contact sheets, and drift policies.

Use `tools/piskel-mcp/character_piskel_pipeline.py` for import, export,
validation, audit, and preview generation. Production sheets must remain
traceable to a registered editable source.
