# Editable approved merchant activities

Each `.piskel` contains seven ordered frames:

`work`, `rare`, `player`, `inspect`, `habit`, `signature`, `showcase`.

The six rejected quiet concepts are not present in these production sources.
Every frame uses a 512 x 512 canvas, a fixed scale, lower-body root X 256, and
foot baseline Y 496.
