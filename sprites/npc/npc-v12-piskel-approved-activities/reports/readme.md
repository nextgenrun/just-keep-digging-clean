# Approved activity Piskel QA

Per-merchant previews, contact sheets, alignment overlays, metadata, and drift
reports. Root drift must remain at or below one source pixel, the bottom
baseline must remain exact, and no opaque pixel may touch a canvas edge.
