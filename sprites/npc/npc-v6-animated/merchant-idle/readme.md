# Merchant Idle Runtime Videos

Approved transparent WebM loops loaded by `BootScene.preloadNPCs()` and displayed by `NPCManager`. The v5 single-frame merchant images remain loaded as browser-compatibility fallbacks.

