# Native hearth sources

One independent built-in ImageGen output per tier, copied without pixel edits.
These RGB originals retain generation provenance. Background transparency must
be verified on separate runtime outputs; the sources themselves stay intact.
