# Editable merchant idle sources

Each `.piskel` contains the eight planted states for one merchant in production
order: quiet, work, rare, player, inspect, habit, signature, showcase.

These per-merchant documents are the editable sources for the v10 runtime pack.
All frames share one scale and a fixed lower-body root/baseline.
