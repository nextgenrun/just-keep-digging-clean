# Vehicle sprites

Generated and processed player-controllable vehicle artwork. Each version folder keeps source generation output alongside the transparent runtime sprite.

`arc-core-v3/` is the approved production Small/Omega Arc package. Its ten
gameplay roles and one sandbox background are exported from fixed-center
Piskel documents and registered by `values/arcCoreVisuals.sprite.json`.
