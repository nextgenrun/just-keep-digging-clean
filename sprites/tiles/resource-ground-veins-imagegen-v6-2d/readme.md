# Resource ground veins ImageGen V6

Inspectable 188 px lossless WebP references for the new six-variant,
ground-embedded resource family. Production uses the matching packed semantic
and recognition atlases.

These are transparent top-down overlays. They contain no terrain square and do
not own gameplay tile state.
