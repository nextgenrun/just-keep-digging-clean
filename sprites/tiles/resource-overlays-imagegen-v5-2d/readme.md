# Resource overlays ImageGen V5 2D

Isolated 188 px lossless WebP references for the approved transparent resource
overlays. The default scenic renderer uses the matching packed semantic atlas;
these individual files preserve inspectable per-resource outputs and are not
opaque terrain blocks.
