# Heavenblocks v2 sources

The three chroma atlases were generated from the approved concept mockups as
style references only. Their alpha siblings were produced with the installed
ImageGen chroma-key helper. Runtime consumes only the split modular WebP
sprites in the parent biome directories.
