# Level One Identity Kits V1

Thirty native 2x2 RGBA atlases provide four crop-safe pieces for every added
Level One family: terrain buttress, embedded structure, foreground cluster,
and prop group. Runtime selection substitutes one cropped frame into an
existing ground or foreground role placement; it does not add draw density.

Rollback: `?levelOneSourceFamilies=0`.
