# Ground damage alpha sheets

Transparent production intermediates extracted from `../sources/` with the
installed ImageGen chroma-removal helper.

The soft matte, border auto-key, and despill preserve fine dust and narrow
fracture edges while removing the green generation background. The
deterministic builder crops these sheets into 120 safe-inset runtime frames.
